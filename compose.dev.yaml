services:
  frontend:
    image: frontend-bpm-image-dev
    container_name: frontend-bpm-dev
    build:
      context: ./frontend-bpm
      dockerfile: Dockerfile.dev
    develop:
      watch:
        - action: sync
          path: ./frontend-bpm/src
          target: /app/src

  backend:
    ports:
      - 8000:4000


  db:
    ports:
      - "5432:5432"